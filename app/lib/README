赛事结果数据导入
1、判断当前数据库中最后的赛事数据日期，获取要读取数据的日期
2、从网站链接下载指定日期的html文件，文件按照./data/result/2008/6/2008-06-21.html格式存放
3、读取html文件，读取出其中有效数据，并另存为csv文件格式，文件存放在html同一目录下
4、手工修改csv文件中不符合要求的数据，如球队名称后有个“(中)”的情况
5、分析csv文件中的数据
6、读取每一条赛事数据的赛事名称，判断是否已经存在数据库中，如果不存在，则插入
7、读取每一条赛事数据的主客球队名称，判断是否已经存在数据库中，如果不存在，则插入
8、读取每一条赛事数据的赛事信息，插入数据库